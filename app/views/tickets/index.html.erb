<p style="color: green"><%= notice %></p>

<h1>Tickets</h1>

<div id="tickets">
  <% @tickets.each do |ticket| %>
    <%= render ticket %>
    <p>
      <%= link_to "Bekijk de ticket", ticket %>
    </p>
  <% end %>
</div>

<%= link_to "Maak een nieuwe ticket aan", new_ticket_path %>